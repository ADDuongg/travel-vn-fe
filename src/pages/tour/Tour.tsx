// src/pages/Tour.tsx (trích đoạn)
import { MainLayout } from '@/layout';
import Container from '@components/Container';
import React from 'react';
const TourPage: React.FC = () => {
  return (
    <MainLayout>
      {/* Sticky Tabs giữ nguyên */}

      <Container></Container>
    </MainLayout>
  );
};

export default TourPage;
